{% extends "index.html.twig" %}

{% block section %}
<br>
<nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="?">Accueil</a></li>
    <li class="breadcrumb-item active" aria-current="page">Projets</li>
  </ol>
</nav>

<p class="h1">Tous les projets</p>

<table class="table table-hover table-sm"><thead>
<tr><th>Titre</th><th>Description</th><th>Détails</th></tr>
</thead><tbody>
{% for projet in projets %}
      <tr>
            <td>{{projet.titre}}</td><td class="w-50 p-3">{{projet.description}}</td>
            <td> 
                  <form method="post" action="index.php"> 
                        <input type="hidden" class="id_projet" name="id_projet" value="{{projet.id_projet}}"/>
                        <input type="hidden" class="id_cours" name="id_cours" value="{{projet.id_cours}}"/>
                        <input type="submit" class="btn btn-outline-dark" name="details" value="Voir plus"/> 
                  </form>
            </td>
      </tr>
{% endfor %}
</tbody></table>
{# Code ChatGPT + mes modifications #}
<nav aria-label="Page navigation example">
    <ul class="pagination justify-content-center" style="margin:20px 0">
        {% for i in 1..totalPages %}
            <li class="page-item {{ currentPage == i ? ' active' }}">
            <a class="page-link text-dark" href="?action=liste&page={{ i }}">{{ i }}</a></li>
        {% endfor %}
    </ul>
</nav>

{% endblock %}

